# 20250723회고록 

## 📌2156번. 
처음에는 배운 permMain으로 해보려고 했다.
구체적으로는 선택과 미선택으로 가지치기를 하면서 해보려 했는데! 
끝나는 조건과 연속일 때의 조건을 잘 나눠서 해보려 했다. 
시간복잡도가 2^N으로 뛰어서 포기해야 했다.  

결국 메모제이션을 생각하게 되었고 특정 위치 i를 보면 
i를 선택하지 않았을 때 => dp[i-1] 
i, i-1을 선택했을 때 => mat[i] + mat[i-1] + dp[i-3] 
i, i-2를 선택했을 때 => mat[i] + dp[i-2]
이렇게 3가지 경우가 있는 걸 알아내야 했다. 어려웡 ㅠ 

## 10844번. 
자바 1.8 기준 **숫자를 자릿수별로 나누는 방법**

1. 문자열을 변환해서 char 처리 
```java
int num = 1234; 
List<Integer> digits = new ArrayList<>(); 

for(char ch: String.valueOf(num).toCharArray()){
	digits.add(ch - '0'); 
}
```

2. 나눗셈과 나머지로 뒤에서부터 추출 
```
int num = 1234; 
List<Integer> digits = new ArrayList<>(); 

while(num>0){
	digits.add(0, num%10); 
	num /= 10 ;
}
```
3. 배열로 바로 저장하기 
```
int num = 1234; 
String str = Integer.toString(num); 
int[] digits = new int[str.length()]; 

for(int i=0; i<str.length(); i++){
	digits[i] = str.charAt(i) - '0'; 
}
```
